config:
  target: "http://localhost:8080"
  phases:
    - duration: 10
      arrivalRate: 1000
  processor: "./stress-data.js"
scenarios:
  - name: "QnA By Id"
    flow:
      - function: "generateId"
      - get:
          url: "/qa/{{id}}"
